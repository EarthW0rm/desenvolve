{
  "name": "poo",
  "version": "1.0.0",
  "description": "Projeto para aula de POO",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "tsc",
    "start": "npm run compile --noEmitOnError && node ./dist/index.js"

  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.6.2"
  }
}
